[package]
name = "hello"
version = "0.1.0"
edition = "2024"

[lib]
name = "hello"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
log = { workspace = true }
kernel = { path = "../../kernel" }
